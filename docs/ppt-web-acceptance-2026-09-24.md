# PPT 网站闭环验收

本地地址：http://localhost:5184

测试项目：`13edd92d869e37cd7d82643fc7104627`

模型：沿用首页选择 GPT。输入为虚构园区网络升级 DOCX，不含真实客户数据。

## 检查记录

- [x] 首页选择 GPT，进入独立 PPT 制作。
- [x] 网页上传 DOCX 并填写五页制作需求。
- [x] 原生第一阶段确认。网页提交后才进入第二阶段，状态记录只有 stage 1 回执。
- [x] 原生第二阶段确认。5 页、中文讲稿、对象动画、无图片/旁白、连续制作、完整规范审阅；网页将强调色从 #D97706 调整为 #A34413 后提交。
- [x] 完整规范审阅与显式批准。五页正文覆盖材料关键条件；访问策略使用表格，制作约束未混入正文；点击网页“确认此版规范并制作”。
- [x] 逐页制作、讲稿、动画、视觉审查。5 页结构检查无错误/警告，13 个图标、1 个原生表格，GPT 五页视觉审查全部通过，首版原生导出 passed。
- [x] 网页预览及原生批注修改。原生编辑器点选第四页文字、添加标注、应用并提交；网站核对范围并显式确认后由 GPT 执行，重新视觉审查五页全部通过。
- [x] 关闭页面后恢复。第二阶段生成中关闭页面，重新打开原项目链接，GPT 及后台任务状态保留。
- [x] 终稿网页下载及文件结构核查。修订后关闭重开恢复最新状态，点击下载并验证落盘内容。

首版网页下载已验证：`moonwalk_1790225668944.pptx`，44,970 字节，下载文件 SHA-256 与服务器终稿一致。浏览器下载事件等待超时，但文件实际落入 Downloads 且字节校验通过，不能将该事件超时误记为下载失败。

已通过原生元素批注选择第四页 `_edit_23`，要求明确“每次变更均具备责任人、维护窗口和回退步骤”；保存批注后，网站进入待确认状态并收起旧下载。核对范围后显式确认执行，未更改规范、大纲或全局设计。

## 最终结果

**本次 GPT 本地网页闭环验收通过。**

- 项目状态 `complete`，视觉审查 `passed`，修订状态 `applied`；首版和修订版均为五页全部通过。
- 最终文件：`/Users/keyiyu/Downloads/moonwalk_1790226088205.pptx`，45,048 字节，下载文件 SHA-256 与服务端终稿完全一致。
- PPTX 内包含 5 页、5 页中文备注、1 个可编辑 `<a:tbl>` 表格、5 页对象动画；第四页实际包含“每次变更均具备”新文字。
- SHA-256 比对：只有 `04_验收与回退.svg` 改变；其余四页、`design_spec.md`、`spec_lock.md` 均与首版完全一致。
- 最终网页预览已人工检查修改处，两行正文无明显重叠；原生批注列表清空，未自动新增修改。
- 全程由网站发起上传、两次原生确认、完整规范批准、元素批注、修改确认与两次下载；未使用直接接口伪造批准或绕过审查。真实模型调用仅使用 GPT。
- 此轮未更改生成代码或 PPT-master 本体，仅新增验收记录。
- 收尾自动化回归：89 项中 88 项通过、0 失败，1 项需显式启用的独立真实 GPT 审查脚本跳过；本轮真实模型验收由上述网页闭环完成。覆盖原学习材料上传、知识检测、开放式问答、权限、原生确认、修订与导出；导出副本复测为 passed，45,048 字节。

## 边界

- 这是一个五页合成 DOCX 场景，不等于全部文件规模、模板、图片生成等分支全部验收。
- 未在桌面 PowerPoint 中逐页播放；可编辑表格、备注和动画以导出文件结构验证，网页画布做人工视觉检查。
- 关闭重开的是浏览器页面，没有退出整个浏览器进程或重启服务。
- 正式 Linux/Render 执行环境、持久存储及定时七天清理的生产验收尚未执行；未部署正式网站。

验收不改原生工作流、不自动绕过确认、不关闭已启用的审查；不涉及正式部署。
